// Set this to the Wild and The Sun CentralPass backend URL at launch.
// Leave it empty until that venue's backend is provisioned. Never point this
// site at another venue's backend or put private API keys in this file.
window.WILD_SUN_CONFIG = Object.freeze({ centralpassApiBase: '' });
